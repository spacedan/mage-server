version: "3"

services:
  mongo: 
    image: mongo:latest
    volumes: 
    - mongodata:/data/db
    ports: 
    - "27017:27017"
  mage:
    image: mage:latest
    links:
    - mongo
    ports:
    - "4242:4242"

volumes:
  mongodata:
